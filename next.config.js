/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // necesario para que funcione el export estático
    reactStrictMode: true,
  }
  
  module.exports = nextConfig
  